# 数据表映射关系

## 核心业务表

### 用户与权限

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `users` | 平台用户（身份来自 tauth） | eng, name, team, roles, status, knot_token |
| `permission_requests` | 权限申请与审批 | eng, apply_roles, status, operator |

### 标签

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `tags` | 策略标签主表 | id, name, precision_val, sample_count, rank_no |

### 工单

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `tickets` | 工单主表 | id, title, tag_id, status, submitter, handler, category, result_type |
| `ticket_timeline` | 工单时间线 | ticket_id, action, op |
| `ticket_drafts` | 工单草稿 | owner, ticket_id, step, revoked |

### 结论沉淀

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `sediments` | 结论沉淀 | ticket_id, title, tag_id, category, result_type |
| `sediment_updates` | 结论更新记录 | sediment_id, by_user, reason |

### 反馈

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `feedback` | 意见反馈 | id, user, eng, type, content, status |

### 审计

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `audit_logs` | 审计日志 | user, action, target, ip |

---

## 数据导入与评测

### 元信息

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `data_meta` | 数据元信息（全局共享） | key, value, updated_by |
| `data_import_history` | 数据导入历史 | file_name, dataset_name, status, imported_by |

### AI评测明细数据（正式）

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `ai_evaluate_detail` | Adata AI评测明细（同结构兜底） | policy_ids, ai_evaluate_policy_ids, element_fingerprint, ds |
| `real_data_tags` | 标签级聚合（正式表） | tag_id, tag_name, total, fp, precision_val |
| `real_data_samples` | 样本级明细（正式表） | tag_id, sample_id, **policy_ids**, **ai_evaluate_policy_ids**, media_url, element_fingerprint |
| `real_data_tag_precision` | 策略标签精度（标签级） | tag_id, tag_name, total, fp, tp, precision |
| `real_data_tag_precision_samples` | 策略标签精度（样本级） | tag_id, sample_id, **policy_ids**, **ai_evaluate_policy_ids**, media_url, uid |

### 发布备份（发布前自动备份）

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `publish_backup_real_data_tags` | 发布备份标签级聚合 | publish_version_id, 同 `real_data_tags` |
| `publish_backup_real_data_samples` | 发布备份样本级明细 | publish_version_id, 同 `real_data_samples` |
| `publish_backup_real_data_tag_precision` | 发布备份策略标签精度 | publish_version_id, 同 `real_data_tag_precision` |
| `publish_backup_real_data_tag_precision_samples` | 发布备份策略标签精度样本 | publish_version_id, 同 `real_data_tag_precision_samples` |
| `publish_version` | 发布版本记录 | version, operator, source(draft\|rollback) |

---

## 素材预分类

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `material_category` | 归因分类定义 | tag_id, name, feature, source(manual\|ai) |
| `material_category_relation` | 素材↔分类归属 | tag_id, sample_id, category_id, media_url |
| `material_conclusion` | 分类汇总结论 | tag_id, tag_name, content, summary |

### 上传分类数据

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `uploaded_classify_data` | 用户上传的分类数据 | tag_id, sample_id, **policy_ids**, **ai_evaluate_policy_ids**, batch_id |
| `uploaded_classify_history` | 上传历史记录 | tag_id, file_name, sample_count, uploaded_by |

---

## 其他

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `trag_cache` | TRAG检索缓存 | cache_key(MD5), task, results, created_at |

---

## 关键列名映射说明

### `policy_ids` / `ai_evaluate_policy_ids` 统一映射

这两个字段在**所有表**中保持一致命名，未发生过变更：

| 数据库列名 | 含义 | 前端/API字段名 |
|-----------|------|---------------|
| `policy_ids` | 机器命中策略ID列表 | `machineTag`, `policyIds`, `machine_tag`（多级兜底） |
| `ai_evaluate_policy_ids` | AI评估策略ID列表 | `humanTag`, `aiEvaluatePolicyIds`, `human_tag`（多级兜底） |

涉及的表：
- `real_data_samples`
- `real_data_tag_precision_samples`
- `publish_backup_real_data_samples`
- `publish_backup_real_data_tag_precision_samples`
- `uploaded_classify_data`
- `ai_evaluate_detail`

### 前端多级兜底读取逻辑

```javascript
// policy_ids 兜底
s.policyIds || s.machineTag || s.machine_tag

// ai_evaluate_policy_ids 兜底
r?.humanTag || r?.ai_evaluate_policy_ids
```

