# 误杀样本归因平台——各个功能说明

> 本文档用于沉淀平台当前已实现的所有功能、对应页面、后端接口及核心实现位置，便于后续维护与改造参考。

---

## 一、项目整体架构

| 层级 | 技术栈 | 入口/说明 |
| --- | --- | --- |
| 前端 | Vue 3 + Vite + Pinia + Vue Router + Tailwind CSS | `项目根目录/src/main.js` |
| 后端 | Node.js + Express + MySQL2 | `项目根目录/server/src/index.js` |
| 数据库 | MySQL | 表结构定义：`项目根目录/server/src/db/schema.js` |
| 相似检索微服务 | Python + FastAPI + uvicorn（Node 内部 spawn 管理） | `项目根目录/server/trag_service/main.py` |

核心运行命令：

```bash
# 前端开发
npm run dev
# 后端开发
npm run dev        # server 目录下
npm run init-db    # 初始化数据库
npm run import-adata
```

---

## 二、通用能力

### 2.1 登录与统一身份识别

- 用户通过企业微信统一登录态 `/ts:auth/tauth/info.ashx` 获取身份信息；
- 后端通过 `identify` 中间件从请求头 `x-user-eng / x-user-name / x-user-team` 识别当前用户；
- 用户表 `users` 只管理角色与状态，不保存密码。

相关文件：

- 后端：`项目根目录/server/src/middleware/auth.js`
- 后端：`项目根目录/server/src/routes/auth.js`
- 前端：`项目根目录/src/stores/auth.js`
- 前端：`项目根目录/src/views/LoginView.vue`

### 2.2 角色权限体系

| 角色 | 说明 | 可见模块 |
| --- | --- | --- |
| submitter | 提交人 | 误杀样本分析、素材特征分类、误杀问题提需、结论沉淀 |
| handler | 处理人 | 误杀问题提需、结论沉淀 |
| admin | 管理员 | 全部 + 管理模块 |
| 无角色 | 仅可申请权限 | 申请页、我的权限页 |

权限申请审批通过 `permission_requests` 表流转，管理员在「权限审批」页面处理。

相关文件：

- 后端：`项目根目录/server/src/routes/users.js`
- 前端：`项目根目录/src/views/ApplyView.vue`
- 前端：`项目根目录/src/views/MyPermissionView.vue`
- 前端：`项目根目录/src/views/manage/ApprovalView.vue`

### 2.3 顶部导航与全局反馈

- 顶部展示品牌、一级导航、数据更新时间、意见反馈入口、用户信息下拉菜单；
- 用户下拉支持「企微联系本人」「我的权限」「退出登录」；
- 意见反馈支持上传截图（jpg/png/gif，单张 ≤ 5MB）；
- 通知中心实时轮询工单与审批未读数。

相关文件：

- 前端：`项目根目录/src/components/TheHeader.vue`
- 前端：`项目根目录/src/stores/notification.js`
- 前端：`项目根目录/src/components/ToastHost.vue`
- 后端：`项目根目录/server/src/routes/notifications.js`

---

## 三、误杀样本分析

### 3.1 功能概述

提供标签维度的误杀样本大盘，包括标签列表、精度、样本量、趋势图，支持下钻到标签详情。

### 3.2 页面与路由

- 列表页：`/analysis` → `AnalysisView.vue`
- 详情页：`/analysis/tag/:id` → `TagDetailView.vue`

### 3.3 核心数据表

- `tags`：标签元信息（id、name、precision_val、sample_count、rank_no）。
- `real_data_tag_precision`：AI 评测分析表（策略_标签）标签级聚合精度数据。
- `real_data_tag_precision_samples`：AI 评测分析表样本级明细。
- `real_data_samples`：正式数据样本级明细（旧数据源）。

### 3.4 主要能力

| 能力 | 说明 |
| --- | --- |
| 标签筛选/搜索 | 按关键字、到达时间范围、标签排序 |
| KPI 卡片 | 展示总样本、FP 数、查准率等 |
| 标签列表 | 每行展示标签名、精度、样本量、趋势 mini 图 |
| 标签详情 | 展示该标签下样本列表、时间趋势图、元素类型分布 |
| 导出 Excel | 导出该标签下样本明细为 xlsx |
| 数据口径 | 查准率 = TP / (TP + FP) |
| 样本字段展示 | 样本卡片/列表展示「审核人（ai_evaluate_reviewer_name）、创意ID(DCID)（dc_id）、客户主体名称(OPS)（ops_advertiser_name）、审核物理指纹（md5）（element_fingerprint）」，四个字段取自 `real_data_samples` 表自身字段，`real_data_samples` 表中的这四个字段在数据管理模块上传时从 `ai_evaluate_detail` 表关联映射而来；若 `real_data_samples` 中字段为空，回退到 `ai_evaluate_detail` 关联匹配 |

### 3.5 相关接口

- `GET /api/tags`
- `GET /api/tags/overview`
- `GET /api/tags/:id/samples`
- `GET /api/tags/:id/trend`
- `POST /api/tags/:id/export/excel`

### 3.6 相关文件

- 前端：`项目根目录/src/views/analysis/AnalysisView.vue`
- 前端：`项目根目录/src/views/analysis/TagDetailView.vue`
- 后端：`项目根目录/server/src/routes/tags.js`

---

## 四、素材特征分类

### 4.1 功能概述

将某个标签下的素材按照用户自建分类维度进行归类、标注画面特征、撰写分类结论，并导出 Word/PDF 报告。

### 4.2 页面与路由

- 主页面：`/classify` → `ClassifyView.vue`
- 标注弹窗组件：`PreviewAnnotate.vue`
- 智能分类弹窗：`ChatClassify.vue`
- 分类脚本：`scriptClassify.js`

### 4.3 核心数据表

- `material_category`：分类维度（特征、摘要、详情）。
- `material_category_relation`：样本与分类的归属关系，存储画面特征、补充说明。
- `material_conclusion`：标签级分类结论。
- `uploaded_classify_data`：用户在分类页上传的 Excel/CSV 数据。
- `uploaded_classify_history`：上传历史记录。

### 4.4 主要能力

| 能力 | 说明 |
| --- | --- |
| 数据源切换 | 选择「上传数据」或「平台数据」进行预分类 |
| 一键预分类 | 调用 AI 对选中样本进行自动分类建议 |
| 智能分类对话 | 通过 KNOT 智能体端点进行两阶段素材分类 |
| 分类管理 | 增删改查分类维度 |
| 样本归类 | 将样本指派到某个分类，可编辑画面特征与补充说明 |
| 分类结论 | 按标签撰写并保存汇总结论 |
| 导出报告 | 导出 Word/PDF 报告 |
| 上传数据 | 上传该标签下带分类的样本数据 |
| 清空数据 | 清空指定标签或全部上传数据 |
| 分类下载字段 | 导出的 Excel/报告需包含「审核人（ai_evaluate_reviewer_name）、创意ID(DCID)（dc_id）、客户主体名称(OPS)（ops_advertiser_name）、审核物理指纹（md5）（element_fingerprint）」，四个字段取自 `real_data_samples` 表自身字段，`real_data_samples` 表中的这四个字段在数据管理模块上传时从 `ai_evaluate_detail` 表关联映射而来 |

### 4.5 相关接口

- `GET /api/materials/:tagId/categories`
- `POST /api/materials/:tagId/categories`
- `PUT /api/materials/:tagId/categories/:catId`
- `DELETE /api/materials/:tagId/categories/:catId`
- `POST /api/materials/:tagId/assign`
- `GET /api/materials/:tagId/conclusion`
- `POST /api/materials/:tagId/conclusion`
- `POST /api/materials/:tagId/export/word`
- `POST /api/materials/:tagId/export/pdf`
- 素材特征分类上传相关：`/api/classify-upload/*`

### 4.6 相关文件

- 前端：`项目根目录/src/views/classify/ClassifyView.vue`
- 前端：`项目根目录/src/views/classify/PreviewAnnotate.vue`
- 前端：`项目根目录/src/views/classify/ChatClassify.vue`
- 前端：`项目根目录/src/views/classify/scriptClassify.js`
- 后端：`项目根目录/server/src/routes/materials.js`
- 后端：`项目根目录/server/src/routes/classifyUpload.js`

---

## 五、误杀问题提需（工单系统）

### 5.1 功能概述

围绕误杀样本创建工单，经过提交、受理、处理、结论、采纳/驳回/重开等状态流转，并沉淀结论。

### 5.2 页面与路由

- 工单列表/详情：`/tickets` → `TicketsView.vue`

### 5.3 核心数据表

- `tickets`：工单主表。
- `ticket_timeline`：工单操作时间线。

### 5.4 主要能力

| 能力 | 说明 |
| --- | --- |
| 创建工单 | 选择标签、元素类型、行业、紧急度、样本等 |
| 受理工单 | handler 或 admin 受理 |
| 处理结论 | 填写结论、原因、分类、是否需要标签等 |
| 采纳/驳回/重开 | submitter 对处理结果进行操作 |
| 删除工单 | admin 可删除 |
| 工单状态变更通知 | 通过企业微信机器人 Webhook 通知相关人员 |
| 工单样本字段 | 工单内「一键输出」字段需包含「审核人（ai_evaluate_reviewer_name）、创意ID(DCID)（dc_id）、客户主体名称(OPS)（ops_advertiser_name）、审核物理指纹（md5）（element_fingerprint）」 |

### 5.5 相关接口

- `GET /api/tickets`
- `GET /api/tickets/:id`
- `POST /api/tickets`
- `POST /api/tickets/:id/accept`
- `POST /api/tickets/:id/conclude`
- `POST /api/tickets/:id/adopt`
- `POST /api/tickets/:id/reject`
- `POST /api/tickets/:id/reopen`
- `DELETE /api/tickets/:id`

### 5.6 相关文件

- 前端：`项目根目录/src/views/tickets/TicketsView.vue`
- 后端：`项目根目录/server/src/routes/tickets.js`
- 后端：`项目根目录/server/src/services/wecomNotify.js`

---

## 六、结论沉淀

### 6.1 功能概述

对历史工单的处理结论进行汇总沉淀，形成可检索、可复用的知识库。

### 6.2 页面与路由

- 沉淀库：`/sediment` → `SedimentView.vue`

### 6.3 核心数据表

- `sediments`：沉淀结论主表。
- `sediment_updates`：沉淀结论更新记录。

### 6.4 主要能力

| 能力 | 说明 |
| --- | --- |
| 沉淀列表 | 展示所有已采纳的工单结论 |
| 搜索 | 按关键字、标签、行业、类型等检索 |
| 详情/更新 | 查看沉淀详情，管理员可更新并记录原因 |
| 删除 | admin 可删除 |
| 统计 | KPI 与分类分布 |
| 标签云 | 展示沉淀涉及的热门标签 |
| 沉淀样本字段 | 结论沉淀的样本明细展示/导出需包含「审核人（ai_evaluate_reviewer_name）、创意ID(DCID)（dc_id）、客户主体名称(OPS)（ops_advertiser_name）、审核物理指纹（md5）（element_fingerprint）」 |

### 6.5 相关接口

- `GET /api/sediments`
- `GET /api/sediments/:id`
- `POST /api/sediments/:id/update`
- `DELETE /api/sediments/:id`
- `GET /api/sediments/search`
- `GET /api/sediments/tags`
- `GET /api/sediments/stat/kpi`
- `GET /api/sediments/stat/distribution`

### 6.6 相关文件

- 前端：`项目根目录/src/views/sediment/SedimentView.vue`
- 后端：`项目根目录/server/src/routes/sediments.js`

---

## 七、管理模块

### 7.1 数据大盘

- 展示平台核心数据指标，如工单量、沉淀量、用户数等。
- 页面：`/manage/dashboard` → `DashboardView.vue`
- 接口：`GET /api/dashboard`
- 后端：`项目根目录/server/src/routes/dashboard.js`

### 7.2 数据管理

- 上传 AI 评测明细数据（分批上传，解决大文件 413 问题）。
- 上传 AI 评测分析表（策略_标签）数据集。
- 查看数据导入历史。
- 删除已上传数据（可按类型删除）。
- 字段关联状态持久化。
- 页面：`/manage/data` → `DataManageView.vue`
- 后端：`项目根目录/server/src/routes/dataMeta.js`
- 核心接口：
  - `GET /api/data-meta`
  - `POST /api/data-meta/upload`
  - `POST /api/data-meta/upload-init`
  - `POST /api/data-meta/upload-batch`
  - `POST /api/data-meta/upload-finalize`
  - `POST /api/data-meta/upload-precision`
  - `GET /api/data-meta/precision-tags`
  - `GET /api/data-meta/precision-tags/:id/samples`
  - `DELETE /api/data-meta`
  - `GET /api/data-meta/field-status`
  - `POST /api/data-meta/field-status`

### 7.3 用户管理

- 查看平台用户列表。
- 修改用户角色、状态、KNOT Token。
- 页面：`/manage/users` → `UsersView.vue`
- 后端：`项目根目录/server/src/routes/users.js`
- 接口：
  - `GET /api/manage/users`
  - `PUT /api/manage/users/:id`

### 7.4 权限审批

- 查看用户提交的权限申请。
- 通过或驳回申请。
- 页面：`/manage/approval` → `ApprovalView.vue`
- 接口：
  - `GET /api/manage/requests`
  - `PUT /api/manage/requests/:id`

### 7.5 审计日志

- 记录关键操作日志（登录、增删改等）。
- 页面：`/manage/audit` → `AuditView.vue`
- 后端：`项目根目录/server/src/routes/audit.js`
- 接口：`GET /api/audit`

### 7.6 意见反馈管理

- 查看用户提交的意见反馈。
- 管理员回复反馈。
- 页面：`/manage/feedback` → `FeedbackView.vue`
- 后端：`项目根目录/server/src/routes/feedback.js`
- 接口：
  - `GET /api/feedback`
  - `PUT /api/feedback/:id`

---

## 八、AI 能力

### 8.1 功能概述

平台接入 KNOT 智能体端点，提供素材分类、结论生成等 AI 辅助能力。

### 8.2 主要能力

| 能力 | 说明 |
| --- | --- |
| 模型列表 | 获取可用 AI 模型列表 |
| AI 生成 | 调用智能体生成分类建议、结论文案等 |
| Token 使用统计 | 查看 AI 调用量 |
| 智能分类对话 | 素材特征分类中的两阶段分类交互 |

### 8.3 相关接口

- `GET /api/ai/models`
- `POST /api/ai/generate`
- `GET /api/ai/usage`

### 8.4 相关文件

- 后端：`项目根目录/server/src/routes/ai.js`
- 后端：`项目根目录/server/src/services/aiService.js`
- 前端：`项目根目录/src/views/classify/ChatClassify.vue`

---

## 九、TRAG 相似素材检索

### 9.1 功能概述

调用 TRAG 检索服务，根据文本或素材特征检索相似素材，支持 7 种任务类型。

### 9.2 页面与组件

- 弹窗组件：`TragSearchModal.vue`

### 9.3 核心数据表

- `trag_cache`：检索结果缓存，24 小时过期。

### 9.4 主要能力

| 能力 | 说明 |
| --- | --- |
| 任务类型选择 | 7 种任务类型 |
| 文本/素材检索 | 支持文本输入或选择平台样本作为输入 |
| 筛选 | 按标签、一级行业、二级行业、过滤表达式筛选 |
| 结果展示 | 展示相似样本列表、相似度、来源等 |
| 缓存机制 | 相同查询命中缓存直接返回 |

### 9.5 相关接口

- `GET /api/trag/tasks`
- `POST /api/trag/search`

### 9.6 相关文件

- 前端：`项目根目录/src/components/TragSearchModal.vue`
- 后端：`项目根目录/server/src/routes/trag.js`
- 后端：`项目根目录/server/src/trag/manager.js`
- Python 服务：`项目根目录/server/trag_service/main.py`
- Python SDK：`项目根目录/server/trag_service/trag_client.py`

---

## 十、媒体代理

### 10.1 功能概述

后端提供 `/api/media-proxy` 代理 HTTP/HTTPS 媒体资源，解决 HTTPS 页面加载 HTTP 资源、CDN 跨域、TLS 握手超时、鉴权过期等问题。

### 10.2 主要能力

- 协议翻转重试（https → http）；
- 多种 Referer/Origin Header 组合重试；
- CDN 鉴权参数剥离后重试；
- 支持 HTTP Range 请求；
- 失败时返回 SVG 占位图。

### 10.3 相关文件

- 后端：`项目根目录/server/src/index.js`

---

## 十一、素材特征分类上传

### 11.1 功能概述

专门用于素材特征分类模块的数据上传，与数据管理中的 AI 评测数据上传独立。

### 11.2 核心数据表

- `uploaded_classify_data`
- `uploaded_classify_history`

### 11.3 主要能力

| 能力 | 说明 |
| --- | --- |
| 单标签上传 | 上传某个标签下的样本数据 |
| 分批上传 | 大样本量分批写入，避免请求体过大 |
| 失败重试 | 记录失败批次，支持单独重试 |
| 去重 | 按样本 ID + 指纹去重 |
| 状态查询 | 查询某标签是否有上传数据 |
| 导出 Excel | 导出该标签下上传数据 |
| 清空数据 | 清空指定标签或全部数据 |

### 11.4 相关接口

- `POST /api/classify-upload/upload`
- `POST /api/classify-upload/batch`
- `POST /api/classify-upload/retry-failed`
- `GET /api/classify-upload/:tagId/samples`
- `GET /api/classify-upload/:tagId/status`
- `GET /api/classify-upload/history`
- `DELETE /api/classify-upload/all`
- `DELETE /api/classify-upload/:tagId`
- `POST /api/classify-upload/:tagId/export/excel`

### 11.5 相关文件

- 后端：`项目根目录/server/src/routes/classifyUpload.js`
- 前端：`项目根目录/src/api/index.js` 中的 `classifyUploadApi`

---

## 十二、待改造方案（已记录）

- 误杀数据改造方案：`项目根目录/误杀数据改造方案.md`
  - 目标：将素材特征分类的「平台数据」替换为「AI 评测标签明细数据」作为预分类来源；
  - 新增 AI 评测标签明细的上传与导出入口；
  - 复用 `uploaded_classify_data` 表，新增 `data_source_type` 字段区分来源。

---

## 十三、关键文件索引

| 文件 | 作用 |
| --- | --- |
| `项目根目录/src/router/index.js` | 前端路由定义 |
| `项目根目录/src/api/index.js` | 前端 API 封装 |
| `项目根目录/src/stores/auth.js` | 用户身份状态 |
| `项目根目录/server/src/index.js` | Express 后端入口 |
| `项目根目录/server/src/db/schema.js` | 数据库表结构 |
| `项目根目录/server/src/db/pool.js` | MySQL 连接池 |
| `项目根目录/server/src/middleware/auth.js` | 登录鉴权中间件 |

