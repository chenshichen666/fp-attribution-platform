# 经常犯的错误 / 顺利预览的正确处理

> 记录本全栈项目（前端 Vite + 后端 Express + Python TRAG 微服务）在预览环节反复踩过的坑，以及当前已验证可顺利预览的正确做法。

## 一、本地预览的正确启动方式

项目为全栈架构，必须前后端同时拉起，否则页面会一直白屏或接口 502。

- 前端 Vite 端口：`36111`
- 后端 Express 端口：`8787`
- Python TRAG 微服务端口：`9100`
- 启动命令：`npm run dev`（通过 `dev-server.mjs` 同时拉起前端与后端）
- 必须先确认 `36111` / `8787` 端口未被占用，否则先 kill 旧 `dev-server.mjs` 进程，避免加载到旧代码
- `vite.config` 中 `server.allowedHosts` 必须包含预览域名（`*.preview.platform.example.com`）
- 验证启动成功：终端日志显示 Vite 就绪 + MySQL 连通 + 建表/迁移完成

## 二、素材无法预览（线上/本地黑屏）的两个根因

### 坑 1：media-proxy 高并发请求被中断

- 错误做法：使用 `node-fetch` + 自定义 `keepAlive agent` + `AbortSignal.timeout` 代理素材资源。
- 现象：页面同时加载多个视频素材时，`node-fetch` 存在竞态问题，多个请求的 `AbortController` 信号相互干扰，部分请求抛出 `AbortError`，最终触发占位符或超时，表现为素材黑屏无法打开。实测 30 个并发请求有 10 个因此中断。
- 正确处理：
  - 改用 Node 20 内置的 `globalThis.fetch`，移除自定义 `agent` 与 `AbortSignal.timeout`
  - 超时改用 `AbortController`，成功读取到响应头后立即 `clearTimeout`，避免流式读取响应体时被意外中止
  - 信号量上限从 15 提高到 40，并移除主动丢弃排队请求的超时逻辑
  - 修复后 30 个并发请求全部成功

### 坑 2：samples 接口回退条件过于严格

- 错误做法：`/api/tags/:id/samples` 优先读取 `real_data_tag_precision_samples` 表，主表为空时回退到 `real_data_samples` 表的回退条件被 `precisionFpCount > 0` 限制。
- 现象：对于 `fp=0` 的标签，即使 `real_data_samples` 表里有 `is_fp=1` 的素材，也无法被查询到，导致该标签下素材列表为空、无法预览。
- 正确处理：主表 `real_data_tag_precision_samples` 为空时直接回退查询 `real_data_samples`（仍保留 `is_fp=1` 过滤，保证数据口径一致）。修复后 `fp=0` 标签也能正确返回真实素材。

## 三、确认"能顺利预览"的验证清单

1. `dev-server` 日志显示 Vite 就绪、MySQL 连通、建表/迁移完成
2. 访问 `/api/tags/:id/samples` 返回非 0 条素材（修复坑 2 后）
3. `media-proxy` 能拉取真实 CDN 视频（返回正常视频流，非占位符、非超时）（修复坑 1 后）
4. 通过预览网关外网域名访问时，若 `curl` 返回 `code=000`，属于沙箱网络无法解析外网域名的问题，与本次代码修复无关，本地 36111 的代理链路已验证正常
