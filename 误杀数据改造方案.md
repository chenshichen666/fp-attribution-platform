# 误杀数据改造方案

## 一、改造背景与目标

当前素材特征分类页面依赖「平台数据」作为一键预分类的数据来源之一，但该数据来源路径与业务实际诉求不匹配。本次改造目标：

- 将「平台数据」替换为「AI 评测标签明细数据」作为预分类来源；
- 新增 AI 评测标签明细数据的「上传入口」与「导出入口」；
- 让一键预分类仅基于「上传数据」和「AI 评测标签明细数据」两类数据源；
- 复用现有 `uploaded_classify_data` 表，通过新增 `data_source_type` 字段区分不同来源，降低改造范围。

## 二、数据源定义

| 数据源类型 | data_source_type | 说明 |
| --- | --- | --- |
| 普通上传数据 | `upload` | 用户通过素材特征分类页面上传的历史数据 |
| AI 评测标签明细数据 | `ai_eval` | 通过管理模块新入口上传的、带有人审标签的明细数据 |

## 三、数据库表结构变更

复用表：`uploaded_classify_data`

新增字段：

```sql
ALTER TABLE uploaded_classify_data
ADD COLUMN data_source_type VARCHAR(32) NOT NULL DEFAULT 'upload'
COMMENT '数据来源类型：upload（普通上传）、ai_eval（AI评测标签明细）';
```

存量数据默认归为 `upload` 类型，确保兼容性。

## 四、前端页面改造

### 4.1 素材特征分类页

数据源选择器调整：

- 移除 `platform` 选项；
- 仅保留 `upload` 和 `ai-eval` 两个选项；
- `ai-eval` 选项读取 `uploaded_classify_data` 表中 `data_source_type = 'ai_eval'` 的数据。

### 4.2 管理模块 - 新增上传入口

在「数据管理」页面新增：

- 上传按钮：AI 评测标签明细数据上传；
- 上传文件格式：xlsx；
- 覆盖策略：按标签全量覆盖（同一标签多次上传，以最新一次为准，不追加）；
- 解析后写入 `uploaded_classify_data`，`data_source_type = 'ai_eval'`。

### 4.3 管理模块 - 新增导出入口

在「数据管理」页面新增：

- 导出按钮：按标签导出 AI 评测标签明细数据；
- 导出文件名示例：`14776_FP数据_6字段.xlsx`；
- 仅包含三种元素类型：图片、视频、文本；
- 每个元素类型占用一个独立 sheet；
- 导出字段：
  1. 到达时间（天）
  2. 审核元素类型
  3. 审核标签 ID
  4. AI 评测人审标签
  5. 审核元素值
  6. 审核物理指纹（md5）

## 五、后端接口改造

### 5.1 数据查询接口

调整素材特征分类相关查询逻辑：

- 查询 `ai-eval` 类型数据时，条件为 `data_source_type = 'ai_eval'`；
- 查询 `upload` 类型数据时，条件为 `data_source_type = 'upload'`；
- 统一使用 COALESCE 兜底显示字段（如 `reviewer_name` 等）。

### 5.2 新增接口

- `POST /api/ai-eval-detail/upload`：上传 AI 评测标签明细 xlsx；
- `POST /api/ai-eval-detail/export`：按标签导出 6 字段 xlsx；
- `GET /api/ai-eval-detail/list`：查询已上传的 AI 评测标签明细列表。

### 5.3 一键预分类逻辑

一键预分类时只从以下两类数据取样本：

- `upload`：普通上传数据；
- `ai_eval`：AI 评测标签明细数据（原 `platform` 数据源不再使用）。

## 六、关键字段说明

- `AI 评测人审标签`：对应 `ai_evaluate_policy_ids` JSON 数组字符串，导出时保持原格式 `[1001]`，不剥离括号；
- `审核物理指纹（md5）`：对应 `elementFingerprint`；
- `到达时间（天）`：按天粒度聚合 `arrive_time`；
- `审核元素类型`：仅保留图片、视频、文本三种。

## 七、后续待确认事项

1. 老「平台数据」相关接口与代码是否彻底下线，还是先保留兼容开关；
2. AI 评测标签明细上传模板 xlsx 的列名与顺序是否需要固定；
3. 导出入口的 UI 形式：是放在数据管理页每个标签行内，还是统一放在一个导出面板中；
4. 是否需要对 `ai_eval` 数据做预览、删除等管理能力。
